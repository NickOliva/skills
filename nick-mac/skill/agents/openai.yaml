interface:
  display_name: "Nick Mac"
  short_description: "Review and align Nick's evolving Mac build"
  default_prompt: "Use $nick-mac to compare this Mac with my build manifest, review differing items, and prepare one alignment plan for me to accept."

policy:
  allow_implicit_invocation: false
